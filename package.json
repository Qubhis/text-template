{
    "name": "text-template",
    "version": "1.0.0",
    "description": "text template app",
    "private": true,
    "scripts": {
        "build": "npm run build:backend && npm run build:frontend",
        "build:backend": "tsc",
        "build:frontend": "cp -r src/frontend/styles src/frontend/index.html ./dist/frontend",
        "start": "node dist/backend/server.js",
        "dev": "nodemon --exec tsx src/backend/server.ts",
        "dev:build": "tsc --watch",
        "clean": "rm -rf dist",
        "clean:deep": "rm -rf dist node_modules package-lock.json && npm install",
        "type-check": "tsc --noEmit",
        "lint": "echo 'Linting not configured yet'",
        "test": "echo 'Tests not configured yet'",
        "validate": "npm run type-check && npm run lint && npm run test",
        "prebuild": "npm run validate",
        "docker:build": "docker build -t text-template ."
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "cors": "^2.8.5",
        "express": "^5.1.0",
        "typescript": "^5.8.3"
    },
    "devDependencies": {
        "@types/cors": "^2.8.19",
        "@types/express": "^5.0.3",
        "@types/node": "^24.0.7",
        "nodemon": "^3.1.10",
        "prettier": "^3.6.2",
        "tsx": "^4.20.3"
    }
}
